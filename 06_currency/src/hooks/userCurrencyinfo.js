import {useEffect, useState} from "react"

//its a general naming convention for hooks to use prefix "use" 
function useCurrencyInfo(currency){
    //we want to call an api
    useEffect(() =>{
        fetch()
    },[])

}